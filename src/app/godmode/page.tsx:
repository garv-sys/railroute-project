import { GodModeWorkspace } from "@/components/tools/GodModeWorkspace";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "GodMode — RailRoute",
  description:
    "Full-spectrum route intelligence: all direct trains + top 15 split journeys, zero filters, zero caps.",
};

export default function GodModePage() {
  return <GodModeWorkspace />;
}
